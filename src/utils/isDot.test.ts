import {isDot} from "./isDot";

test ("a entrada deve ser igual à um ponto", () => {
    expect(isDot).toBe(".");
});

test ("a entrada não deve ser igual à um ponto", () => {
    expect(isDot).toBe(".");
});